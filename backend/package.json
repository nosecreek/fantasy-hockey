{
  "name": "fantasy-hockey",
  "version": "1.0.0",
  "description": "Yahoo Fantasy Hockey Companion App",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production node app.js",
    "dev": "NODE_ENV=development nodemon app.js",
    "build:ui": "rm -rf build && cd ../frontend && npm run build && cp -r build ../backend"
  },
  "author": "Dustin Lammiman",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "yahoo-fantasy": "github:nosecreek/yahoo-fantasy-sports-api"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}
